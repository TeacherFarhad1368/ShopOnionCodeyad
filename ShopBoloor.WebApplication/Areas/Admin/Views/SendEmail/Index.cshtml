@model List<Emails.Application.Contract.SensEmailApplication.Query.SendEmailQueryModel>
@using Shared.Domain.Enum;
@{
    ViewData["Title"] = "لیست ایمیل های ارسالی خبر نامه";
}
<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <hr />
                <a class="btn btn-success" asp-action="Create">
                    <i class=" fa fa-plus mx-2"></i> ایمیل جدید
                </a>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
                <table id="example1" class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th> عنوان</th>
                            <th>تاریخ ثبت</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @if(Model.Count() > 0)
                        {
                            foreach (var item in Model)
                            {
                                <tr id="Deleted_@item.Id">
                                    <td>
                                        @item.Title
                                    </td>
                                    <td>
                                        @item.CreationDate
                                    </td>
                                    <td>
                                        <a class="btn btn-ifo" title="جزییات ایمیل" asp-action="Detaill" asp-route-id="@item.Id">
                                            <i class="fa fa-info"></i>
                                        </a>

                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <th> عنوان</th>
                            <th>تاریخ ثبت</th>
                            <th></th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
@section Scripts{
    <script>
        $(function () {
            $('#example1').DataTable({
                'paging': true,
                'lengthChange': true,
                'searching': true,
                'ordering': true,
                'info': true,
                'autoWidth': true
            })
        })
    </script>
}